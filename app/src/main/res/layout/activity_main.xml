<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212"
    tools:context=".MainActivity">

    <!-- HEADER BAR -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/headerContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:background="#000000"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/lblTranslateTo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Translate to"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <RadioGroup
            android:id="@+id/languageRadioGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginStart="16dp"
            app:layout_constraintStart_toEndOf="@id/lblTranslateTo"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <RadioButton
                android:id="@+id/radioItalian"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🇮🇹"
                android:textSize="28sp"
                android:textColor="#FFFFFF"
                android:button="@null"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:paddingHorizontal="8dp"
                android:alpha="1.0"/>

            <RadioButton
                android:id="@+id/radioPortuguese"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🇧🇷"
                android:textSize="28sp"
                android:textColor="#FFFFFF"
                android:button="@null"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:paddingHorizontal="8dp"
                android:alpha="0.4"/>
        </RadioGroup>

        <ImageButton
            android:id="@+id/btnSettings"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@android:drawable/ic_menu_preferences"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:tint="#FFFFFF"
            android:scaleType="fitCenter"
            android:padding="8dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- STATUS TEXT -->
    <TextView
        android:id="@+id/statusText"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="Waiting for configuration..."
        android:textAlignment="center"
        android:textSize="20sp"
        android:textColor="#DDDDDD"
        android:gravity="center"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/headerContainer"
        app:layout_constraintBottom_toTopOf="@+id/progressBar"
        app:layout_constraintEnd_toStartOf="@+id/thresholdContainer"
        app:layout_constraintStart_toStartOf="parent" />

    <FrameLayout
        android:id="@+id/thresholdContainer"
        android:layout_width="60dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/headerContainer"
        app:layout_constraintBottom_toTopOf="@+id/footerContainer"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="4dp">

        <TextView
            android:id="@+id/lblThresholdValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0.5s"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:layout_gravity="top|center_horizontal"
            android:layout_marginTop="8dp"/>

        <SeekBar
            android:id="@+id/silenceThresholdSeekBar"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:rotation="270"
            android:max="12"
            android:progress="2"
            android:progressTint="#FFFFFF"
            android:thumbTint="#FFFFFF" />
            
    </FrameLayout>

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:visibility="invisible"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/statusText"
        app:layout_constraintBottom_toTopOf="@+id/footerContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- FOOTER CONTAINER PER I 5 BOTTONI -->
    <LinearLayout
        android:id="@+id/footerContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingBottom="24dp"
        android:paddingTop="8dp"
        android:paddingStart="18dp"
        android:paddingEnd="18dp"
        android:clipToPadding="false"
        app:layout_constraintBottom_toBottomOf="parent">

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="0.35" />

        <!-- BOTTONE PLAY (VERDE) - Avvia Processo -->
        <ImageButton
            android:id="@+id/btnPlayAction"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/play"
            android:background="@drawable/circle_background"
            android:backgroundTint="#388E3C"
            android:scaleType="centerInside"
            android:padding="14dp"
            android:elevation="6dp"/>

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1.8" />

        <!-- BOTTONE PAUSE (GIALLO) - Mette in Pausa -->
        <ImageButton
            android:id="@+id/btnPause"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/pause"
            android:background="@drawable/circle_background"
            android:backgroundTint="#FBC02D"
            android:scaleType="centerInside"
            android:padding="14dp"
            android:elevation="6dp"/>

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1.8" />

        <!-- BOTTONE STOP (ROSSO) - Conclude sessione ed esce dall'app -->
        <ImageButton
            android:id="@+id/btnStop"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/stop"
            android:background="@drawable/circle_background"
            android:backgroundTint="#D32F2F"
            android:scaleType="centerInside"
            android:padding="14dp"
            android:elevation="6dp"/>

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1.8" />

        <!-- BOTTONE RESET (ARANCIONE) - Pulisce tutto -->
        <ImageButton
            android:id="@+id/btnReset"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/reset"
            android:background="@drawable/circle_background"
            android:backgroundTint="#F57C00"
            android:scaleType="centerInside"
            android:padding="14dp"
            android:elevation="6dp"/>

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1.8" />

        <!-- BOTTONE TRANSLATE (AZZURRO) - Force Cut -->
        <ImageButton
            android:id="@+id/btnTranslateNow"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/translate_now"
            android:background="@drawable/circle_background"
            android:backgroundTint="#03A9F4"
            android:scaleType="centerInside"
            android:padding="14dp"
            android:elevation="6dp"/>

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="0.35" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
